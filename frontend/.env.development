# Loaded automatically by Vite in dev mode (`npm run dev`) — production builds
# (`npm run build`) do not read this file. Without it the axios baseURL is
# undefined and every API call falls through to the Vite dev server itself,
# which answers with index.html and breaks the app after login.
VITE_API_BASE_URL=http://localhost:8080/api/v1
