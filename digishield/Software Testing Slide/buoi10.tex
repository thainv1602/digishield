%!TEX program = xelatex
\documentclass[aspectratio=169,12pt]{beamer}
\hfuzz=2pt
\vfuzz=2pt
\hbadness=10000
\vbadness=10000

%---------------------------------------------------------------------
% Font & encoding
%---------------------------------------------------------------------
\usepackage{fontspec}
\usepackage{polyglossia}
\setmainlanguage{vietnamese}
\IfFontExistsTF{Noto Sans}
  {\setmainfont{Noto Sans}\setsansfont{Noto Sans}}
  {\setmainfont{Helvetica Neue}\setsansfont{Helvetica Neue}}
\IfFontExistsTF{DejaVu Sans Mono}
  {\setmonofont{DejaVu Sans Mono}}
  {\setmonofont{Menlo}}

%---------------------------------------------------------------------
% Theme
%---------------------------------------------------------------------
\usetheme{Copenhagen}
\usecolortheme{default}
\usefonttheme{professionalfonts}

% Màu chủ đạo: xanh #428BCA
\definecolor{darkgray}{HTML}{428BCA}
\definecolor{midgray}{HTML}{428BCA}
\definecolor{lightgray}{RGB}{230,230,230}
\definecolor{accentgray}{HTML}{428BCA}

\setbeamercolor{palette primary}{bg=darkgray,fg=white}
\setbeamercolor{palette secondary}{bg=midgray,fg=white}
\setbeamercolor{palette tertiary}{bg=accentgray,fg=white}
\setbeamercolor{palette quaternary}{bg=darkgray,fg=white}
\setbeamercolor{structure}{fg=darkgray}
\setbeamercolor{section in toc}{fg=darkgray}
\setbeamercolor{alerted text}{fg=darkgray}
\setbeamercolor{block title}{bg=darkgray,fg=white}
\setbeamercolor{block body}{bg=lightgray,fg=black}
\setbeamercolor{block title alerted}{bg=midgray,fg=white}
\setbeamercolor{block body alerted}{bg=lightgray,fg=black}
\setbeamercolor{block title example}{bg=accentgray,fg=white}
\setbeamercolor{block body example}{bg=lightgray,fg=black}
\setbeamercolor{frametitle}{bg=darkgray,fg=white}
\setbeamercolor{title}{fg=white}
\setbeamercolor{subtitle}{fg=lightgray}
\setbeamercolor{author}{fg=black}
\setbeamercolor{date}{fg=darkgray}
\setbeamercolor{footline}{fg=white}
\setbeamercolor{headline}{bg=darkgray}

% Loại bỏ navigation symbols
\setbeamertemplate{navigation symbols}{}

% Footline tùy chỉnh
\setbeamertemplate{headline}{}
\setbeamertemplate{footline}{%
  \leavevmode%
  \hbox{%
    \begin{beamercolorbox}[wd=.5\paperwidth,ht=1.8ex,dp=0.7ex,left,leftskip=1em]{palette primary}%
      \usebeamerfont{author in head/foot}Kiểm thử Phần mềm --- Buổi 10
    \end{beamercolorbox}%
    \begin{beamercolorbox}[wd=.5\paperwidth,ht=1.8ex,dp=0.7ex,right,rightskip=1em]{palette primary}%
      \usebeamerfont{date in head/foot}
      \insertframenumber{}/\inserttotalframenumber
    \end{beamercolorbox}%
  }%
}

% Itemize
\setbeamertemplate{itemize item}{\textbullet}
\setbeamertemplate{itemize subitem}{--}
\setbeamertemplate{itemize subsubitem}{\(\circ\)}
\setbeamerfont{itemize/enumerate body}{size=\normalsize}
\setbeamerfont{itemize/enumerate subbody}{size=\small}

% Compact spacing + auto slide-break for long frames
\addtobeamertemplate{block begin}{\vspace{-0.3em}}{}
\addtobeamertemplate{block end}{}{\vspace{-0.4em}}
\addtobeamertemplate{block alerted begin}{\vspace{-0.3em}}{}
\addtobeamertemplate{block alerted end}{}{\vspace{-0.4em}}
\addtobeamertemplate{block example begin}{\vspace{-0.3em}}{}
\addtobeamertemplate{block example end}{}{\vspace{-0.4em}}
\setlength{\parskip}{0pt}
\setbeamertemplate{frametitle continuation}{\normalfont\small~(tiếp)}

%---------------------------------------------------------------------
% Packages
%---------------------------------------------------------------------
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{listings}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,fit}
\usepackage{array}
\usepackage{xcolor}

\lstset{
  basicstyle=\ttfamily\footnotesize,
  breaklines=true,
  frame=single,
  framesep=3pt,
  aboveskip=3pt,
  belowskip=1pt,
  numbers=none,
  showstringspaces=false,
  escapeinside={(*@}{@*)},
  backgroundcolor=\color{lightgray},
  rulecolor=\color{midgray},
  keywordstyle=\bfseries,
  commentstyle=\upshape\color{midgray},
}

%---------------------------------------------------------------------
% Metadata
%---------------------------------------------------------------------
\title{Selenium --- Kiểm thử tự động giao diện web (phần 1)}
\subtitle{Buổi 10 / 12 --- Môn: Kiểm thử Phần mềm}
\author{}

%=====================================================================
\begin{document}
%=====================================================================

%---------------------------------------------------------------------
% SLIDE 1 - Title
%---------------------------------------------------------------------
\begin{frame}
  \begin{center}
    {\bfseries\normalsize TRƯỜNG ĐẠI HỌC GIAO THÔNG VẬN TẢI\\[-0.1em]
    PHÂN HIỆU TẠI TP.HCM}\\[0.2em]
    {\bfseries\small BỘ MÔN CÔNG NGHỆ THÔNG TIN}\\[0.35em]
    \includegraphics[height=0.95cm]{image/LogoUTC.jpg}\hspace{0.08cm}
    \includegraphics[height=0.95cm]{image/logo.jpg}

    \vspace{0.6em}
    \begin{beamercolorbox}[sep=8pt,center]{title}
      \usebeamerfont{title}\inserttitle\par
      \vspace{0.2em}
      {\usebeamerfont{subtitle}\usebeamercolor[fg]{subtitle}\insertsubtitle\par}
    \end{beamercolorbox}

    \vspace{0.5em}
    {\usebeamerfont{author}\usebeamercolor[fg]{author}\insertauthor\par}
    \vspace{0.2em}
    {\usebeamerfont{date}\usebeamercolor[fg]{date}\insertdate\par}
  \end{center}
\end{frame}

%---------------------------------------------------------------------
% SLIDE 2 - Nội dung
%---------------------------------------------------------------------
\begin{frame}{Nội dung buổi học}
  \begin{enumerate}
    \item Vị trí trong đề cương --- vì sao cần kiểm thử UI tự động
    \item Selenium là gì: lịch sử, thành phần, kiến trúc WebDriver
    \item So sánh nhanh Selenium --- Cypress --- Playwright
    \item Các bước cài đặt: project test riêng, Maven/Gradle, Selenium Manager
    \item Chuẩn bị hệ thống thực hành DigiShield (backend + frontend)
    \item Điều khiển browser: \texttt{get}, \texttt{getTitle}, \texttt{navigate}, \texttt{window}
    \item \textbf{Locators}: \texttt{By.id}, CSS selector, XPath --- chiến lược chọn locator
    \item Thao tác element: \texttt{sendKeys}, \texttt{click}, \texttt{getText}, \texttt{getAttribute}
    \item Test đăng nhập DigiShield hoàn chỉnh với JUnit 5
    \item Debug: screenshot, page source, DevTools
    \item Thực hành và bài nộp
  \end{enumerate}
\end{frame}

%=====================================================================
\section{Giới thiệu --- Vì sao kiểm thử UI tự động}
%=====================================================================

%--- SLIDE 3 ----------------------------------------------------------
\begin{frame}{Vị trí trong đề cương môn học}
  \begin{block}{Chương 6 --- Công cụ kiểm thử tự động Selenium}
    Buổi 10 và 11 thuộc \textbf{Chương 6} của đề cương:
    giới thiệu Selenium, khái niệm chung, các bước cài đặt,
    và xây dựng kịch bản kiểm thử giao diện web tự động.
  \end{block}

  \begin{center}
  \small
  \begin{tabular}{clc}
    \toprule
    \textbf{Chương} & \textbf{Nội dung} & \textbf{Buổi} \\
    \midrule
    C4 & JUnit, Mockito, integration test & 5--7 \\
    C5 & Kiểm thử hộp đen, API testing (Postman) & 8--9 \\
    \textbf{C6} & \textbf{Selenium --- kiểm thử UI tự động} & \textbf{10--11} \\
    C7 & Kiểm thử trong thực tiễn, tổng kết BTL & 12 \\
    \bottomrule
  \end{tabular}
  \end{center}
  \vspace{-0.4em}

  \begin{exampleblock}{Buổi hôm nay (phần 1)}
    Cài đặt Selenium WebDriver, tìm element bằng locator,
    thao tác form và viết test đăng nhập đầu tiên cho DigiShield.
  \end{exampleblock}
\end{frame}

%--- SLIDE 4 ----------------------------------------------------------
\begin{frame}{Hành trình kiểm thử DigiShield đến hôm nay (1/2)}
  \begin{center}
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabularx}{\textwidth}{cllX}
    \toprule
    \textbf{Buổi} & \textbf{Tầng} & \textbf{Công cụ} & \textbf{Đối tượng test} \\
    \midrule
    5--6 & Unit & JUnit 5, Mockito & \texttt{Interception\allowbreak ServiceImpl}, \texttt{StubAiClient} \\
    7 & Integration & @SpringBootTest, Testcontainers & RLS đa tenant, sự kiện \\
    8--9 & API & Postman, Newman & \texttt{/api/v1/auth}, \texttt{/interventions} \\
    \textbf{10--11} & \textbf{E2E / UI} & \textbf{Selenium WebDriver} & \textbf{Trang web như người dùng} \\
    \bottomrule
  \end{tabularx}
  \end{center}
\end{frame}

%--- SLIDE 4b ---------------------------------------------------------
\begin{frame}{Hành trình kiểm thử DigiShield đến hôm nay (2/2)}
  \begin{block}{Mảnh ghép cuối cùng}
    Ta đã test \textbf{logic} (unit), \textbf{tích hợp} (integration) và
    \textbf{HTTP endpoint} (API). Tầng còn lại: mô phỏng
    \textbf{người dùng thật} mở browser, chọn vai trò, bấm nút
    \emph{Đăng nhập}\ldots{} và kiểm tra hệ thống hoạt động đúng.
  \end{block}

  \begin{alertblock}{Thực trạng DigiShield}
    Repo đã có module \texttt{:e2e} (Selenium, 1 kịch bản
    \texttt{AnalystBlocksAccountE2E}) chạy như \textbf{cổng chặn} trong CI
    --- nhưng để \textbf{đọc hiểu và mở rộng} nó (yêu cầu BTL), hôm nay ta
    học Selenium từ số 0, tự viết lại bộ test đăng nhập.
  \end{alertblock}
\end{frame}

%--- SLIDE 5 ----------------------------------------------------------
\begin{frame}{Vì sao cần kiểm thử UI tự động?}
  \setbeamerfont{itemize/enumerate body}{size=\footnotesize}
  \begin{columns}[T]
    \column{0.5\textwidth}
    \begin{block}{Chỉ test bằng tay thì sao?}
      \begin{itemize}
        \item Mỗi lần release phải bấm lại \textbf{toàn bộ} luồng:
              đăng nhập 4 vai trò, báo cáo phishing, watchlist\ldots
        \item Lặp đi lặp lại $\to$ nhàm chán $\to$ \textbf{bỏ sót}
        \item Regression thủ công tốn nhiều giờ mỗi lần sửa code
        \item Không chạy được trong CI/CD ban đêm
      \end{itemize}
    \end{block}

    \column{0.5\textwidth}
    \begin{block}{UI test tự động}
      \begin{itemize}
        \item Máy ``đóng vai'' người dùng: mở browser, gõ phím, click
        \item Chạy lại hàng trăm lần, kết quả \textbf{nhất quán}
        \item Phát hiện lỗi tích hợp frontend--backend mà unit/API test bỏ lọt
        \item Là lớp \textbf{smoke test} trước khi release
      \end{itemize}
    \end{block}
  \end{columns}

  \vspace{0.2em}
  {\small \textbf{Ví dụ lỗi chỉ UI test bắt được}: API \texttt{POST /auth/login}
  trả 200 đúng, nhưng frontend quên điều hướng sang dashboard $\to$ API test
  xanh, người dùng vẫn ``kẹt'' ở trang login.}
\end{frame}

%--- SLIDE 6 ----------------------------------------------------------
\begin{frame}{Vị trí E2E trên Testing Pyramid}
  \begin{center}
  \begin{tikzpicture}[scale=0.78, every node/.style={font=\small}]
    % Unit (day)
    \fill[lightgray] (1.2,0) -- (10.8,0) -- (9.4,1.5) -- (2.6,1.5) -- cycle;
    \node at (6,0.75) {\textbf{Unit} --- JUnit, Mockito (buổi 5--6)};
    % Integration / API
    \fill[darkgray!45] (2.7,1.6) -- (9.3,1.6) -- (7.9,3.1) -- (4.1,3.1) -- cycle;
    \node at (6,2.35) {\textbf{Integration / API} (buổi 7--9)};
    % E2E
    \fill[darkgray] (4.2,3.2) -- (7.8,3.2) -- (6.6,4.6) -- (5.4,4.6) -- cycle;
    \node[white] at (6,3.85) {\textbf{E2E / UI}};
    % Side labels
    \draw[->, >=Stealth, thick, darkgray] (0.6,0.3) -- (0.6,4.3)
      node[midway, left, align=center, font=\scriptsize]
      {chi phí\\độ dễ vỡ\\thời gian chạy};
    \draw[->, >=Stealth, thick, darkgray] (11.4,4.3) -- (11.4,0.3)
      node[midway, right, align=center, font=\scriptsize]
      {số lượng\\tốc độ\\cô lập lỗi};
    \node[align=left, font=\scriptsize, anchor=west] at (7.6,4.35)
      {$\leftarrow$ Selenium (buổi 10--11)};
  \end{tikzpicture}
  \end{center}

  \begin{alertblock}{Nguyên tắc}
    E2E nằm ở \textbf{đỉnh} tháp: \textbf{ít} test nhất, \textbf{đắt} nhất,
    chậm nhất --- nhưng mỗi test bao phủ \textbf{cả hệ thống} và mang
    \textbf{giá trị nghiệp vụ cao} nhất.
  \end{alertblock}
\end{frame}

%--- SLIDE 7 ----------------------------------------------------------
\begin{frame}{Đặc điểm của tầng E2E --- viết ít nhưng viết đúng chỗ}
  \begin{columns}[T]
    \column{0.5\textwidth}
    \begin{block}{Vì sao E2E ``đắt''?}
      \begin{itemize}
        \item Cần \textbf{cả hệ thống chạy thật}: backend + frontend + browser
        \item Chậm: mỗi test vài giây đến vài chục giây
        \item Giòn: đổi giao diện nhỏ có thể làm vỡ test
        \item Lỗi khó cô lập: fail ở đâu trong cả chuỗi?
      \end{itemize}
    \end{block}

    \column{0.5\textwidth}
    \begin{block}{Nên viết E2E cho gì?}
      \begin{itemize}
        \item Các luồng \textbf{sống còn} (critical path):
              đăng nhập, báo cáo phishing, chặn giao dịch
        \item Happy path chính + vài kịch bản tiêu cực quan trọng
        \item \textbf{Không} dùng E2E để kiểm tra mọi biên
              --- việc đó của unit test (EP/BVA buổi 8)
      \end{itemize}
    \end{block}
  \end{columns}

  \vspace{0.3em}
  \begin{exampleblock}{Với DigiShield}
    Buổi này: luồng \textbf{đăng nhập} (cửa ngõ của mọi vai trò).
    Buổi 11: kịch bản E2E nhiều bước (analyst xử lý báo cáo, watchlist).
  \end{exampleblock}
\end{frame}

%=====================================================================
\section{Selenium là gì?}
%=====================================================================

%--- SLIDE 8 ----------------------------------------------------------
\begin{frame}{Selenium --- lịch sử tóm tắt}
  \begin{center}
  \footnotesize
  \begin{tabularx}{\textwidth}{c>{\raggedright\arraybackslash}X}
    \toprule
    \textbf{Năm} & \textbf{Sự kiện} \\
    \midrule
    2004 & Jason Huggins (ThoughtWorks) tạo Selenium Core --- JavaScript tiêm vào trang \\
    2006 & Simon Stewart phát triển WebDriver --- điều khiển browser ``từ bên ngoài'' \\
    2009 & Hai dự án \textbf{công bố hợp nhất} (Selenium + WebDriver) \\
    2011 & Phát hành \textbf{Selenium 2.0 (Selenium WebDriver)} \\
    2018 & Giao thức WebDriver trở thành \textbf{chuẩn W3C} chính thức \\
    2021 & \textbf{Selenium 4}: dùng thuần giao thức W3C, bỏ JSON Wire cũ \\
    2022 & Selenium 4.6: \textbf{Selenium Manager} --- tự động tải driver \\
    \bottomrule
  \end{tabularx}
  \end{center}

  {\small \textbf{Vì sao tên ``Selenium''?} Đối thủ thời đó là công cụ thương
  mại Mercury; nguyên tố selen (Selenium) là\ldots{} thuốc giải độc thủy ngân
  (Mercury).\\[0.25em]
  \textbf{Điểm cần nhớ}: môn học dùng \textbf{Selenium 4.x} + Java --- giao
  thức chuẩn W3C, không cần tải \texttt{chromedriver} thủ công.}
\end{frame}

%--- SLIDE 9 ----------------------------------------------------------
\begin{frame}{Bộ công cụ Selenium --- ba thành phần}
  \begin{center}
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{p{2.9cm}p{6.3cm}p{3.4cm}}
    \toprule
    \textbf{Thành phần} & \textbf{Vai trò} & \textbf{Dùng khi} \\
    \midrule
    \textbf{WebDriver} & Thư viện lập trình (Java, Python, C\#, JS\ldots) điều khiển browser qua giao thức W3C & Viết test tự động trong code --- \textbf{trọng tâm môn học} \\
    \textbf{IDE} & Extension Chrome/Firefox: ghi lại thao tác (record \& playback), xuất code & Prototype nhanh, người không lập trình \\
    \textbf{Grid} & Phân tán test lên nhiều máy / browser / phiên bản chạy song song & Chạy bộ test lớn trong CI, test đa trình duyệt \\
    \bottomrule
  \end{tabular}
  \end{center}

  \begin{alertblock}{Trong môn học}
    Ta dùng \textbf{Selenium WebDriver + Java + JUnit 5} --- tận dụng toàn bộ
    kỹ năng JUnit/AssertJ đã học từ buổi 5.
    IDE và Grid chỉ cần biết khái niệm.
  \end{alertblock}
\end{frame}

%--- SLIDE 10 ---------------------------------------------------------
\begin{frame}{Kiến trúc Selenium WebDriver (chuẩn W3C)}
  \begin{center}
  \begin{tikzpicture}[
    box/.style={draw=darkgray, thick, rounded corners, align=center,
                minimum height=1.55cm, minimum width=3.2cm, font=\small,
                fill=lightgray},
    lbl/.style={font=\scriptsize, align=center, darkgray},
    arr/.style={<->, >=Stealth, thick, draw=darkgray}]
    \node[box] (test) {\textbf{Test code}\\Java + JUnit 5\\\texttt{selenium-java}};
    \node[box, right=2.1cm of test] (drv) {\textbf{Driver}\\\texttt{chromedriver}\\(process riêng)};
    \node[box, right=2.1cm of drv] (br) {\textbf{Browser}\\Chrome};
    \draw[arr] (test) -- node[lbl, above]{W3C WebDriver\\HTTP + JSON} (drv);
    \draw[arr] (drv) -- node[lbl, above]{giao thức nội bộ\\của browser} (br);
    \node[lbl, below=0.35cm of drv] {Selenium Manager tự tải driver đúng phiên bản (từ 4.6)};
  \end{tikzpicture}
  \end{center}

  \begin{itemize}
    \item Mỗi lệnh (\texttt{driver.get(...)}, \texttt{click()}\ldots) là một
          \textbf{HTTP request} gửi tới driver; driver dịch sang lệnh riêng của browser.
    \item Driver do \textbf{chính hãng browser} cung cấp:
          \texttt{chromedriver} (Google), \texttt{geckodriver} (Mozilla)\ldots
    \item Nhờ chuẩn W3C, \textbf{cùng một test code} chạy được trên
          Chrome, Firefox, Edge, Safari --- chỉ đổi driver.
  \end{itemize}
\end{frame}

%--- SLIDE 11 ---------------------------------------------------------
\begin{frame}{So sánh nhanh: Selenium --- Cypress --- Playwright}
  \begin{center}
  \scriptsize
  \setlength{\tabcolsep}{3pt}
  \begin{tabular}{>{\raggedright\arraybackslash}p{2.5cm}>{\raggedright\arraybackslash}p{3.4cm}>{\raggedright\arraybackslash}p{3.2cm}>{\raggedright\arraybackslash}p{3.4cm}}
    \toprule
     & \textbf{Selenium} & \textbf{Cypress} & \textbf{Playwright} \\
    \midrule
    Ngôn ngữ & Java, Python, C\#, JS, Ruby, Kotlin & JavaScript / TypeScript & JS/TS, Python, Java, .NET \\
    Kiến trúc & Ngoài browser, giao thức \textbf{chuẩn W3C} & Chạy \textbf{bên trong} browser & Ngoài browser, giao thức riêng (CDP) \\
    Browser & Mọi browser chính (driver chính hãng) & Họ Chromium, Firefox, WebKit (hạn chế) & Chromium, Firefox, WebKit \\
    Auto-wait & Không (tự cấu hình wait) & Có sẵn & Có sẵn \\
    Điểm mạnh & Chuẩn hóa, đa ngôn ngữ, cộng đồng lớn nhất, Grid & DX tốt, debug trực quan & Nhanh, API hiện đại \\
    \bottomrule
  \end{tabular}
  \end{center}

  {\small \textbf{Vì sao môn học chọn Selenium?} Theo \textbf{đề cương}
  (Chương 6), là chuẩn W3C, phổ biến nhất trong tuyển dụng tester tại VN;
  dùng \textbf{Java + JUnit 5} --- liền mạch buổi 5--7, không học thêm ngôn
  ngữ mới.\\[0.25em]
  \textbf{Lời khuyên nghề nghiệp}: học vững Selenium $\to$ khái niệm locator,
  wait, Page Object chuyển sang Cypress/Playwright rất nhanh.}
\end{frame}

%=====================================================================
\section{Các bước cài đặt Selenium}
%=====================================================================

%--- SLIDE 12 ---------------------------------------------------------
\begin{frame}{Cài đặt --- yêu cầu môi trường}
  \begin{block}{Checklist trước khi bắt đầu}
    \begin{enumerate}
      \item \textbf{JDK 17+} (máy đã có --- dùng cho DigiShield backend)
      \item \textbf{Google Chrome} bản mới (Selenium Manager sẽ tự khớp driver)
      \item \textbf{IDE}: IntelliJ IDEA / VS Code / Eclipse
      \item \textbf{Maven hoặc Gradle} --- buổi này trình bày cả hai
      \item Mã nguồn DigiShield (backend + frontend) chạy được ở local
    \end{enumerate}
  \end{block}

  \begin{alertblock}{Không cần}
    \begin{itemize}
      \item \textbf{Không} cần tải \texttt{chromedriver.exe} thủ công (Selenium $\geq$ 4.6)
      \item \textbf{Không} cần Docker cho buổi này (backend chạy profile dev với H2)
    \end{itemize}
  \end{alertblock}
\end{frame}

%--- SLIDE 13 ---------------------------------------------------------
\begin{frame}[fragile]{Bước 1 --- Tạo project test riêng}
  \begin{block}{Vì sao project riêng?}
    E2E test đứng \textbf{ngoài} hệ thống, nhìn DigiShield như hộp đen qua browser
    --- giống Postman collection ở buổi 9. Không trộn vào repo backend/frontend.
  \end{block}

\begin{lstlisting}[language={}]
digishield-e2e/
 |-- pom.xml            (hoac build.gradle.kts)
 `-- src
     `-- test
         `-- java
             `-- e2e
                 |-- FirstSeleniumTest.java
                 `-- LoginTest.java
\end{lstlisting}

  \begin{itemize}
    \item Toàn bộ code nằm ở \texttt{src/test/java} --- đây là project ``chỉ để test''
    \item Đặt tên package gợi nghĩa: \texttt{e2e}, \texttt{e2e.auth}, \texttt{e2e.soc}\ldots
  \end{itemize}
\end{frame}

%--- SLIDE 14 ---------------------------------------------------------
\begin{frame}[fragile]{Bước 2a --- Khai báo dependency với Maven}
  \begin{exampleblock}{\texttt{pom.xml} --- phần \texttt{dependencies}}
\begin{lstlisting}[language=xml,basicstyle=\ttfamily\scriptsize]
<dependency>
  <groupId>org.seleniumhq.selenium</groupId>
  <artifactId>selenium-java</artifactId>
  <version>4.27.0</version>
</dependency>
<dependency>
  <groupId>org.junit.jupiter</groupId>
  <artifactId>junit-jupiter</artifactId>
  <version>5.11.4</version>
  <scope>test</scope>
</dependency>
\end{lstlisting}
  \end{exampleblock}

  \begin{itemize}
    \item \texttt{selenium-java}: gói tổng, kéo về WebDriver cho mọi browser + Selenium Manager
    \item \texttt{junit-jupiter}: JUnit 5 quen thuộc từ buổi 5 (assertions, lifecycle)
    \item Nhớ thêm \texttt{maven-surefire-plugin} bản mới để chạy JUnit 5
  \end{itemize}
\end{frame}

%--- SLIDE 15 ---------------------------------------------------------
\begin{frame}[fragile]{Bước 2b --- Khai báo dependency với Gradle (Kotlin DSL)}
  \begin{exampleblock}{\texttt{build.gradle.kts} --- giống cấu trúc DigiShield backend}
\begin{lstlisting}[language={},basicstyle=\ttfamily\scriptsize]
plugins { java }

repositories { mavenCentral() }

dependencies {
  testImplementation(
    "org.seleniumhq.selenium:selenium-java:4.27.0")
  testImplementation(platform(
    "org.junit:junit-bom:5.11.4"))
  testImplementation("org.junit.jupiter:junit-jupiter")
  testRuntimeOnly(
    "org.junit.platform:junit-platform-launcher")
}

tasks.test { useJUnitPlatform() }
\end{lstlisting}
  \end{exampleblock}
\end{frame}

%--- SLIDE 16 ---------------------------------------------------------
\begin{frame}{Bước 3 --- Selenium Manager lo phần driver}
  \begin{columns}[T]
    \column{0.5\textwidth}
    \begin{block}{Trước Selenium 4.6 (cách cũ)}
      \begin{enumerate}
        \item Xem phiên bản Chrome đang cài
        \item Tải \texttt{chromedriver} \textbf{đúng} phiên bản đó
        \item Đặt vào PATH hoặc \texttt{System.setProperty(...)}
        \item Chrome tự cập nhật $\to$ driver lệch $\to$ test vỡ hàng loạt
      \end{enumerate}
    \end{block}

    \column{0.5\textwidth}
    \begin{block}{Từ Selenium 4.6 (cách mới)}
      \begin{enumerate}
        \item Chỉ cần \texttt{new ChromeDriver()}
        \item \textbf{Selenium Manager} (đi kèm trong \texttt{selenium-java})
              tự phát hiện Chrome, tự tải driver khớp phiên bản, tự cache
      \end{enumerate}
    \end{block}
  \end{columns}

  \vspace{0.3em}
  \begin{alertblock}{Lưu ý khi đọc tài liệu cũ trên mạng}
    Hướng dẫn bảo tải \texttt{chromedriver.exe} hoặc dùng thư viện
    WebDriverManager bên thứ ba là tài liệu \textbf{trước 2022} ---
    với Selenium 4.x hiện tại thì không cần nữa.
  \end{alertblock}
\end{frame}

%--- SLIDE 17 ---------------------------------------------------------
\begin{frame}[fragile]{Bước 4 --- Test ``khởi động'' đầu tiên}
  \begin{exampleblock}{\texttt{FirstSeleniumTest.java}}
\begin{lstlisting}[language=java,basicstyle=\ttfamily\scriptsize]
import org.junit.jupiter.api.Test;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

class FirstSeleniumTest {

  @Test
  void moBrowserVaDongLai() {
    // Selenium Manager tu tai driver o lan chay dau
    WebDriver driver = new ChromeDriver();

    driver.get("https://www.selenium.dev");
    System.out.println(driver.getTitle());

    driver.quit(); // luon dong browser khi xong
  }
}
\end{lstlisting}
  \end{exampleblock}
\end{frame}

%--- SLIDE 18 ---------------------------------------------------------
\begin{frame}{Chạy thử --- và các lỗi cài đặt thường gặp}
  {\small \textbf{Chạy}: IDE bấm Run trên method test \textbullet{}
  Maven \texttt{mvn test} \textbullet{} Gradle \texttt{./gradlew test}.
  Kỳ vọng: Chrome bật lên, mở trang, in tiêu đề, tự đóng --- test
  \textbf{xanh}.}

  \vspace{0.1em}

  \begin{center}
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{>{\raggedright\arraybackslash}p{5.9cm}>{\raggedright\arraybackslash}p{6.3cm}}
    \toprule
    \textbf{Triệu chứng} & \textbf{Nguyên nhân / cách xử lý} \\
    \midrule
    Lần chạy đầu rất lâu & Selenium Manager đang tải driver --- bình thường \\
    \texttt{NoSuchDriverException} & Chưa cài Chrome, hoặc mạng chặn tải driver \\
    Test không được nhận diện & Thiếu JUnit platform / surefire cũ --- kiểm tra lại bước 2 \\
    Browser mở rồi ``đứng'' mãi & Quên \texttt{driver.quit()} ở test trước --- đóng process thủ công \\
    \bottomrule
  \end{tabular}
  \end{center}

  {\small \textbf{\texttt{quit()} khác \texttt{close()}}: \texttt{close()} chỉ
  đóng \textbf{tab} hiện tại; \texttt{quit()} đóng \textbf{toàn bộ} browser và
  kết thúc phiên WebDriver. Trong test luôn dùng \texttt{quit()}.}
\end{frame}

%=====================================================================
\section{Chuẩn bị hệ thống DigiShield}
%=====================================================================

%--- SLIDE 19 ---------------------------------------------------------
\begin{frame}[fragile]{Chuẩn bị --- chạy backend DigiShield (profile dev)}
  \begin{exampleblock}{Terminal 1 --- trong thư mục \texttt{digishield/}}
\begin{lstlisting}[language={}]
./gradlew :boot:app:bootRun \
    --args='--spring.profiles.active=dev'
\end{lstlisting}
  \end{exampleblock}

  \begin{itemize}
    \item Profile \textbf{dev}: H2 in-memory, \textbf{không cần Docker},
          dữ liệu demo tự seed
    \item API sẵn sàng tại \texttt{http://localhost:8080/api/v1}
    \item Dev profile \texttt{permitAll} --- \textbf{không cần tài khoản};
          đăng nhập ở frontend chỉ chọn vai trò (slide sau)
  \end{itemize}

  \begin{block}{Nguyên tắc E2E}
    Hệ thống đích phải \textbf{chạy trước} khi test chạy.
    Selenium không khởi động ứng dụng hộ ta --- nó chỉ điều khiển browser.
  \end{block}
\end{frame}

%--- SLIDE 20 ---------------------------------------------------------
\begin{frame}[fragile]{Chuẩn bị --- chạy frontend DigiShield}
  \setbeamerfont{itemize/enumerate body}{size=\small}
  \begin{exampleblock}{Terminal 2 --- trong thư mục \texttt{frontend/}}
\begin{lstlisting}[language={}]
npm install     # lan dau
npm run dev     # Vite dev server, port 5173
\end{lstlisting}
  \end{exampleblock}

  {\scriptsize \textbf{Ghi chú}: bản deploy dùng Cognito; \texttt{npm run dev}
  không đặt \texttt{VITE\_COGNITO\_*} nên \texttt{/login} hiện
  \textbf{dev sign-in form} --- chính là thứ ta sẽ test.}
  \vspace{-0.4em}

  \begin{itemize}
    \item Mở thử bằng tay: \texttt{http://localhost:5173/login}
    \item Ô email \textbf{tự điền} theo vai trò đang chọn
          (\texttt{learner@dev\ldots})
  \end{itemize}

  \begin{alertblock}{Checklist trước khi viết test}
    \begin{enumerate}
      \item Backend trả dữ liệu? $\to$ mở \texttt{http://localhost:8080/api/v1}
      \item Frontend hiển thị trang login? $\to$ mở \texttt{http://localhost:5173/login}
      \item Đăng nhập \textbf{bằng tay} thành công một lần để hiểu luồng
    \end{enumerate}
  \end{alertblock}
\end{frame}

%--- SLIDE 21 ---------------------------------------------------------
\begin{frame}{Đối tượng test hôm nay: trang \texttt{/login} của DigiShield}
  \begin{columns}[T]
    \column{0.52\textwidth}
    \begin{block}{Các thành phần trên trang (LoginPage.tsx)}
      \begin{itemize}
        \item Dropdown \textbf{Vai trò} --- 6 role RBAC
              (mặc định \texttt{learner})
        \item Ô \textbf{Email} --- \textbf{tự điền theo vai trò}
              (\texttt{analyst@dev\ldots}); gõ tay thì thôi tự điền
        \item Nút \textbf{Đăng nhập (dev)} (submit) --- không kiểm tra
              mật khẩu; chỉ xuất hiện khi chưa cấu hình Cognito
      \end{itemize}
    \end{block}

    \column{0.44\textwidth}
    \begin{block}{Điều hướng sau đăng nhập}
      \small
      \begin{tabular}{ll}
        \toprule
        \textbf{Vai trò} & \textbf{Trang đích} \\
        \midrule
        Learner & \texttt{/learn} \\
        Analyst & \texttt{/soc/inbox} \\
        Super Admin & \texttt{/super/tenants} \\
        3 role còn lại & \texttt{/dashboard} \\
        \bottomrule
      \end{tabular}
    \end{block}
    \begin{exampleblock}{Assert của test}
      URL sau đăng nhập \textbf{chứa} đường dẫn đích tương ứng.
    \end{exampleblock}
  \end{columns}
\end{frame}

%--- SLIDE 22 ---------------------------------------------------------
\begin{frame}[fragile]{Nhìn vào DOM của trang login (rút gọn)}
\begin{lstlisting}[language=xml,basicstyle=\ttfamily\scriptsize]
<form>
  <label for="login-role">(*@{\normalfont\scriptsize\smash{Vai trò}}@*)</label>
  <select id="login-role">
    <option value="super_admin">Super Admin</option>
    ...
    <option value="analyst">Analyst</option>
    <option value="learner" selected>Learner</option>
  </select>
  <label for="login-email">Email</label>
  <input id="login-email" type="email" required
         value="learner@dev.digishield.local" />
  <button id="login-submit" type="submit">
    (*@{\normalfont\scriptsize\smash{Đăng nhập (dev)}}@*)</button>
</form>
\end{lstlisting}

  \begin{itemize}
    \item Cả 3 element có \textbf{id ổn định}: \texttt{login-role},
          \texttt{login-email}, \texttt{login-submit} --- ``hợp đồng''
          mà module \texttt{:e2e} cũng bám vào
    \item \texttt{value} của \texttt{option} là \textbf{mã role}
          (\texttt{analyst}) --- bền hơn text hiển thị
  \end{itemize}
\end{frame}

%=====================================================================
\section{Điều khiển browser}
%=====================================================================

%--- SLIDE 23 ---------------------------------------------------------
\begin{frame}[fragile]{Điều khiển browser --- \texttt{get}, \texttt{getTitle}, \texttt{getCurrentUrl}}
\begin{lstlisting}[language=java,basicstyle=\ttfamily\scriptsize]
WebDriver driver = new ChromeDriver();

// Mo trang login cua DigiShield
driver.get("http://localhost:5173/login");

// Tieu de tab hien tai
String title = driver.getTitle();

// URL hien tai - dung de assert dieu huong
String url = driver.getCurrentUrl();
System.out.println(title + " | " + url);

driver.quit();
\end{lstlisting}

  \begin{itemize}
    \item \texttt{get(url)} chờ trang tải xong (sự kiện load) rồi mới trả về
    \item \texttt{getCurrentUrl()} là ``mắt'' chính để kiểm tra \textbf{điều hướng} trong SPA
  \end{itemize}
\end{frame}

%--- SLIDE 24 ---------------------------------------------------------
\begin{frame}[fragile]{Điều khiển browser --- \texttt{manage().window()} và \texttt{navigate()}}
\begin{lstlisting}[language=java,basicstyle=\ttfamily\scriptsize]
// Kich thuoc cua so - UI responsive co the doi layout!
driver.manage().window().maximize();
driver.manage().window().setSize(
    new Dimension(1280, 800));

// Dieu huong nhu nguoi dung bam nut tren browser
driver.navigate().to("http://localhost:5173/login");
driver.navigate().refresh();  // F5
driver.navigate().back();     // nut Back
driver.navigate().forward();  // nut Forward
\end{lstlisting}

  \begin{alertblock}{Kinh nghiệm}
    Luôn cố định kích thước cửa sổ trong \texttt{@BeforeEach}:
    cùng một trang, cửa sổ hẹp có thể ẩn bớt phần tử (menu thu gọn)
    làm locator tìm không thấy --- test ``chập chờn''.
  \end{alertblock}
\end{frame}

%=====================================================================
\section{Locators --- tìm element trên trang}
%=====================================================================

%--- SLIDE 25 ---------------------------------------------------------
\begin{frame}[fragile]{\texttt{WebElement} --- \texttt{findElement} và \texttt{findElements}}
  \begin{block}{Khái niệm}
    \textbf{Locator} = ``địa chỉ'' của một phần tử trong DOM.
    \texttt{findElement(By...)} trả về \texttt{WebElement} đầu tiên khớp;
    không thấy thì ném \texttt{NoSuchElementException} (test fail).
  \end{block}

\begin{lstlisting}[language=java]
// Mot element
WebElement email =
    driver.findElement(By.id("login-email"));

// Danh sach element - khong nem exception,
// tra ve list rong neu khong co gi khop
List<WebElement> options =
    driver.findElements(By.tagName("option"));
// Dropdown vai tro co 6 option (6 role RBAC)
\end{lstlisting}

  \begin{itemize}
    \item \texttt{findElements} + kiểm tra \texttt{isEmpty()} là cách
          kiểm tra ``element có tồn tại không'' mà không làm test nổ
  \end{itemize}
\end{frame}

%--- SLIDE 26 ---------------------------------------------------------
\begin{frame}{Họ locator \texttt{By} --- tổng quan}
  \begin{center}
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{>{\raggedright\arraybackslash}p{3.6cm}>{\raggedright\arraybackslash}p{4.0cm}>{\raggedright\arraybackslash}p{4.9cm}}
    \toprule
    \textbf{Locator} & \textbf{Tìm theo} & \textbf{Ví dụ trên DigiShield} \\
    \midrule
    \texttt{By.id} & Thuộc tính \texttt{id} & \texttt{login-role}, \texttt{login-email}, \texttt{login-submit} \\
    \texttt{By.name} & Thuộc tính \texttt{name} & (trang login không đặt \texttt{name}) \\
    \texttt{By.cssSelector} & Cú pháp CSS & \texttt{option[value='analyst']} \\
    \texttt{By.xpath} & Biểu thức XPath & nút theo text ``Đăng nhập (dev)'' \\
    \texttt{By.linkText} / \texttt{partialLinkText} & Text của thẻ \texttt{<a>} & (trang login không có \texttt{<a>} nào!) \\
    \texttt{By.tagName} & Tên thẻ & \texttt{form}, \texttt{select}, \texttt{option} \\
    \texttt{By.className} & Thuộc tính \texttt{class} & (DigiShield style inline, ít class) \\
    \bottomrule
  \end{tabular}
  \end{center}

  {\small \textbf{Nhận xét từ hệ thống thật}: không phải locator nào ``trong
  sách'' cũng dùng được --- DigiShield không đặt \texttt{name}, ít
  \texttt{class}, ``link'' nhiều nơi là \texttt{span} có \texttt{onClick};
  tester phải \textbf{đọc DOM thật} rồi mới chọn locator.}
\end{frame}

%--- SLIDE 27 ---------------------------------------------------------
\begin{frame}[fragile]{\texttt{By.id} --- locator tốt nhất khi có}
\begin{lstlisting}[language=java]
// Trang login la trang hiem hoi cua DigiShield
// co san id on dinh:
WebElement role =
    driver.findElement(By.id("login-role"));
WebElement email =
    driver.findElement(By.id("login-email"));

// Cung tim duoc bang tagName (kem chinh xac hon)
WebElement form =
    driver.findElement(By.tagName("form"));
\end{lstlisting}

  \begin{block}{Vì sao \texttt{id} là số một?}
    \begin{itemize}
      \item \texttt{id} phải \textbf{duy nhất} trong trang (chuẩn HTML)
      \item Nhanh nhất, dễ đọc nhất, ít vỡ nhất khi giao diện đổi
      \item Còn gắn với \texttt{<label for="...">} $\to$ tốt cả cho accessibility
    \end{itemize}
  \end{block}
\end{frame}

%--- SLIDE 28 ---------------------------------------------------------
\begin{frame}{CSS selector --- cú pháp cần nhớ}
  \begin{center}
  \footnotesize
  \renewcommand{\arraystretch}{0.92}
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{>{\raggedright\arraybackslash}p{5.4cm}>{\raggedright\arraybackslash}p{6.6cm}}
    \toprule
    \textbf{Cú pháp} & \textbf{Ý nghĩa} \\
    \midrule
    \texttt{\#login-email} & Element có id \texttt{login-email} \\
    \texttt{.ten-class} & Element có class \\
    \texttt{button} & Mọi thẻ \texttt{button} \\
    \texttt{button[type='submit']} & \texttt{button} có thuộc tính \texttt{type="submit"} \\
    \texttt{input[placeholder*='x']} & \texttt{placeholder} \textbf{chứa} chuỗi x (\texttt{\textasciicircum=} bắt đầu, \texttt{\$=} kết thúc) \\
    \texttt{form > button} & \texttt{button} là \textbf{con trực tiếp} của \texttt{form} \\
    \texttt{form button} & \texttt{button} là \textbf{hậu duệ} (mọi cấp) của \texttt{form} \\
    \texttt{button:nth-of-type(3)} & Nút thứ 3 cùng cha (dễ vỡ --- hạn chế dùng) \\
    \bottomrule
  \end{tabular}
  \end{center}
  \vspace{-0.4em}

  {\small \textbf{Mẹo}: CSS selector chính là thứ các bạn đã dùng khi viết CSS
  --- và có thể \textbf{thử ngay trong DevTools} trước khi đưa vào test.}
\end{frame}

%--- SLIDE 29 ---------------------------------------------------------
\begin{frame}[fragile]{CSS selector --- ví dụ thật trên DigiShield}
\begin{lstlisting}[language=java]
// Nut submit duy nhat trong form login
driver.findElement(
    By.cssSelector("button[type='submit']"));

// Mot option cu the trong dropdown vai tro
driver.findElement(By.cssSelector(
    "#login-role option[value='analyst']"));

// Trang Watchlist (/soc/watchlist): o Quick Check
// khong co id, nhung co placeholder dac trung
driver.findElement(By.cssSelector(
    "input[placeholder*='domain']"));
\end{lstlisting}

  \begin{itemize}
    \item Placeholder thật của ô Quick Check (trang Watchlist):
          \emph{``Nhập số tài khoản, SĐT, hoặc domain...''} --- ta khớp
          đoạn \texttt{'domain'} trong đó
    \item \texttt{*=} khớp \textbf{một phần} chuỗi --- đỡ vỡ khi câu chữ chỉnh nhẹ,
          nhưng vẫn phụ thuộc ngôn ngữ hiển thị
  \end{itemize}
\end{frame}

%--- SLIDE 30 ---------------------------------------------------------
\begin{frame}{XPath --- cú pháp cần nhớ}
  \begin{center}
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{p{7.7cm}p{5.6cm}}
    \toprule
    \textbf{Cú pháp} & \textbf{Ý nghĩa} \\
    \midrule
    \texttt{//button} & Mọi \texttt{button} ở bất kỳ đâu \\
    \texttt{//input[@id='login-email']} & \texttt{input} có thuộc tính id \\
    \texttt{//option[text()='Analyst']} & Text \textbf{bằng đúng} ``Analyst'' \\
    \texttt{//button[contains(text(),'Đăng')]} & Text \textbf{chứa} chuỗi con \\
    \texttt{//form//button} & \texttt{button} bên trong \texttt{form} \\
    \texttt{//label[@for='login-role']/..} & Đi \textbf{ngược lên} cha (CSS không làm được) \\
    \texttt{(//button)[2]} & Nút thứ 2 trong kết quả (dễ vỡ!) \\
    \bottomrule
  \end{tabular}
  \end{center}

  \begin{block}{Khi nào cần XPath?}
    Khi phải tìm theo \textbf{text hiển thị} hoặc đi \textbf{ngược cây DOM} ---
    hai việc CSS selector không hỗ trợ. Còn lại, ưu tiên CSS (ngắn gọn, nhanh hơn).
  \end{block}
\end{frame}

%--- SLIDE 31 ---------------------------------------------------------
\begin{frame}[fragile]{XPath theo text --- và cái bẫy tiếng Việt}
\begin{lstlisting}[language=java,basicstyle=\ttfamily\scriptsize]
// Nut submit theo text hien thi
driver.findElement(By.xpath(
    "//button[contains(text(),'(*@{\normalfont\scriptsize Đăng nhập}@*)')]"));

// Option theo text hien thi (nhan tieng Anh:
// 'Super Admin', 'Analyst', 'Learner'...)
driver.findElement(By.xpath(
    "//option[text()='Analyst']"));
\end{lstlisting}

  \begin{alertblock}{Ba cái bẫy khi tìm theo text trên DigiShield}
    \begin{enumerate}
      \item Text có \textbf{dấu tiếng Việt} --- file test phải lưu UTF-8, gõ đúng dấu
      \item Cùng trang \texttt{/login}, \textbf{hai chế độ}: bản dev nút ghi
            ``Đăng nhập (dev)'', bản Cognito ghi ``Đăng nhập'' --- text khác,
            \texttt{id} mới là chỗ bám bền
      \item DigiShield có i18n (\texttt{t('Đăng nhập (dev)')}) --- đổi ngôn ngữ giao diện là test vỡ
    \end{enumerate}
  \end{alertblock}
\end{frame}

%--- SLIDE 32 ---------------------------------------------------------
\begin{frame}{Chiến lược chọn locator: id \(>\) CSS \(>\) XPath}
  \begin{center}
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{>{\raggedright\arraybackslash}p{2.4cm}>{\raggedright\arraybackslash}p{3.4cm}>{\raggedright\arraybackslash}p{3.1cm}>{\raggedright\arraybackslash}p{2.8cm}}
    \toprule
    \textbf{Ưu tiên} & \textbf{Locator} & \textbf{Ưu điểm} & \textbf{Nhược điểm} \\
    \midrule
    1 & \texttt{By.id} (hoặc \texttt{data-testid}) & Duy nhất, bền, nhanh & Cần dev đặt sẵn \\
    2 & \texttt{By.cssSelector} & Ngắn gọn, quen thuộc, nhanh & Không tìm theo text \\
    3 & \texttt{By.xpath} & Mạnh nhất (text, đi ngược cây) & Dài, dễ vỡ, chậm hơn \\
    Tránh & Vị trí tuyệt đối, \texttt{nth-child}, class tự sinh & --- & Vỡ ngay khi UI đổi \\
    \bottomrule
  \end{tabular}
  \end{center}

  {\small \textbf{Quy tắc vàng}: locator tốt = \textbf{bền với thay đổi giao
  diện} + \textbf{đọc là hiểu ngay}. Đừng bao giờ copy nguyên XPath tuyệt đối
  \texttt{/html/body/div[2]/div/div[3]/...} từ DevTools vào test!}
\end{frame}

%--- SLIDE 33 ---------------------------------------------------------
\begin{frame}{Thực trạng DigiShield --- khi hệ thống ``không chiều'' tester}
  \begin{block}{Quan sát từ mã nguồn frontend}
    \begin{itemize}
      \item Trang login có \texttt{id} ổn định đủ bộ
            (\texttt{login-role/-email/-submit}); Alert Center có vài
            \texttt{id} lẻ (\texttt{alert-severity}, \texttt{alert-message})
      \item Đa số trang khác (Dashboard, SOC Inbox, Watchlist\ldots):
            input dùng \textbf{style inline}, \textbf{không id}, \textbf{không name}, ít class
      \item Nút bấm chỉ phân biệt được bằng text hoặc vị trí
    \end{itemize}
  \end{block}

  \begin{columns}[T]
    \column{0.5\textwidth}
    \begin{exampleblock}{Chiến lược ngắn hạn (tuần này)}
      Dựa vào thuộc tính \textbf{có nghĩa} đang tồn tại:
      \texttt{type}, \texttt{placeholder} (SOC Inbox), text nút, cấu trúc form.
    \end{exampleblock}

    \column{0.5\textwidth}
    \begin{exampleblock}{Chiến lược dài hạn (đúng bài)}
      \textbf{Đề xuất dev thêm} \texttt{data-testid} vào các element quan trọng
      --- hợp tác dev--tester, chi phí thấp, lợi ích lâu dài.
    \end{exampleblock}
  \end{columns}
\end{frame}

%--- SLIDE 34 ---------------------------------------------------------
\begin{frame}[fragile]{\texttt{data-testid} --- best practice hợp tác dev--tester}
  \begin{exampleblock}{Đề xuất pull request cho các trang SOC (chưa có id)}
\begin{lstlisting}[language={},basicstyle=\ttfamily\scriptsize]
<input  placeholder="Nhap so tai khoan..."
        data-testid="watchlist-quick-check" />
<button data-testid="watchlist-add">
\end{lstlisting}
  \end{exampleblock}

\begin{lstlisting}[language=java,basicstyle=\ttfamily\scriptsize]
// Trong test: chi bam vao "hop dong" testid
driver.findElement(By.cssSelector(
    "[data-testid='watchlist-quick-check']"));
\end{lstlisting}

  {\small \textbf{Vì sao đáng làm?} (1) Tách locator khỏi style/text: refactor
  CSS, đổi câu chữ, i18n \textbf{không} làm vỡ test; (2) là ``hợp đồng'' rõ ràng
  giữa dev và tester --- đổi testid là biết phải sửa test; (3) không ảnh hưởng
  người dùng cuối (thuộc tính \texttt{data-*} trơ với browser).}
\end{frame}

%=====================================================================
\section{Thao tác với element}
%=====================================================================

%--- SLIDE 35 ---------------------------------------------------------
\begin{frame}[fragile]{Nhập liệu --- \texttt{sendKeys} và \texttt{clear}}
\begin{lstlisting}[language=java,basicstyle=\ttfamily\scriptsize]
WebElement email =
    driver.findElement(By.id("login-email"));

// CAN THAN: o email da duoc tu dien san
// (learner@dev.digishield.local). sendKeys
// chi GO THEM vao noi dung cu
// -> phai clear() truoc!
email.clear();
email.sendKeys("tester@digishield.vn");
\end{lstlisting}

  \begin{alertblock}{Lỗi kinh điển với form có prefill}
    Quên \texttt{clear()} $\to$ ô email thành
    \texttt{learner@dev.digishield.localtester@digishield.vn}
    --- test fail khó hiểu.
  \end{alertblock}
\end{frame}

%--- SLIDE 36 ---------------------------------------------------------
\begin{frame}[fragile]{Kích hoạt --- \texttt{click} và \texttt{Select} cho dropdown}
\begin{lstlisting}[language=java]
// Click nut submit (khuyen dung - giong het
// hanh vi nguoi dung)
driver.findElement(By.id("login-submit")).click();

// Dropdown KHONG click nhu nut - dung lop Select:
Select role = new Select(
    driver.findElement(By.id("login-role")));
role.selectByValue("analyst");        // theo ma role
role.selectByVisibleText("Analyst");  // theo nhan
\end{lstlisting}

  \begin{itemize}
    \item \texttt{click()} yêu cầu element \textbf{nhìn thấy được và không bị che};
          bị che thì ném \texttt{ElementClickInterceptedException}
    \item \texttt{Select} (gói \texttt{support.ui}) là API riêng cho
          \texttt{<select>}; ưu tiên \texttt{selectByValue} --- mã role
          không đổi khi giao diện đổi nhãn/ngôn ngữ
  \end{itemize}
\end{frame}

%--- SLIDE 37 ---------------------------------------------------------
\begin{frame}[fragile]{Đọc thông tin --- \texttt{getText} và \texttt{getAttribute}}
\begin{lstlisting}[language=java,basicstyle=\ttfamily\scriptsize]
WebElement submit =
    driver.findElement(By.id("login-submit"));

String label = submit.getText();
// -> "Dang nhap (dev)"
String type = submit.getAttribute("type");
// -> "submit"

WebElement email =
    driver.findElement(By.id("login-email"));
// Voi <input>, noi dung go vao KHONG nam
// trong getText() ma trong thuoc tinh value:
String value = email.getAttribute("value");
// -> "learner@dev.digishield.local" (tu dien)
\end{lstlisting}

  \begin{block}{Ghi nhớ}
    \texttt{getText()} = text \textbf{hiển thị} bên trong thẻ (nút submit trả về
    ``Đăng nhập (dev)''); còn giá trị của \texttt{<input>} phải đọc qua
    \texttt{getAttribute("value")}.
  \end{block}
\end{frame}

%--- SLIDE 38 ---------------------------------------------------------
\begin{frame}[fragile]{Kiểm tra trạng thái --- \texttt{isDisplayed}, \texttt{isEnabled}, \texttt{isSelected}}
  \begin{center}
  \footnotesize
  \begin{tabular}{>{\raggedright\arraybackslash}p{3.2cm}>{\raggedright\arraybackslash}p{8.2cm}}
    \toprule
    \textbf{Method} & \textbf{Trả về \texttt{true} khi} \\
    \midrule
    \texttt{isDisplayed()} & Element đang hiển thị (không \texttt{display:none}/ẩn) \\
    \texttt{isEnabled()} & Element không bị \texttt{disabled} \\
    \texttt{isSelected()} & Checkbox/radio/option đang được chọn \\
    \bottomrule
  \end{tabular}
  \end{center}

\begin{lstlisting}[language=java,basicstyle=\ttfamily\scriptsize]
// isSelected: dung chuan cho <option>
new Select(driver.findElement(
    By.id("login-role"))).selectByValue("analyst");

WebElement analystOpt = driver.findElement(
    By.cssSelector("option[value='analyst']"));
analystOpt.isSelected();  // true - vua chon xong

WebElement learnerOpt = driver.findElement(
    By.cssSelector("option[value='learner']"));
learnerOpt.isSelected();  // false
\end{lstlisting}

  {\small \textbf{Ứng dụng}: assert \textbf{trạng thái form} trước khi submit
  --- chọn nhầm role thì fail ngay tại đây, thay vì fail khó hiểu ở bước
  assert URL phía sau.}
\end{frame}

%=====================================================================
\section{Test đăng nhập DigiShield hoàn chỉnh}
%=====================================================================

%--- SLIDE 39 ---------------------------------------------------------
\begin{frame}{Bước 1 --- đặc tả test case (khuôn mẫu buổi 9)}
  \begin{center}
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{p{1.5cm}p{3.3cm}p{4.5cm}p{3.4cm}}
    \toprule
    \textbf{Mã} & \textbf{Mục tiêu} & \textbf{Dữ liệu} & \textbf{Kết quả mong đợi} \\
    \midrule
    TC-L01 & Đăng nhập Org Admin & role \texttt{org\_admin}, email tự điền & Sang \texttt{/dashboard} \\
    TC-L02 & Analyst \textbf{thấy} hộp báo cáo & role \texttt{analyst} & \texttt{/soc/inbox} + tab THREAT \\
    TC-L03 & Email sai định dạng & \texttt{khong-phai-email} & Ở lại \texttt{/login} \\
    TC-L04 & F5 sau khi đăng nhập & đăng nhập rồi F5 & Giữ phiên? (slide sau!) \\
    \bottomrule
  \end{tabular}
  \end{center}

  \begin{block}{Quy trình không đổi}
    Thiết kế test case (hộp đen --- buổi 8--9) \textbf{trước},
    rồi mới tự động hóa bằng Selenium. Công cụ đổi, tư duy giữ nguyên.
  \end{block}
\end{frame}

%--- SLIDE 40 ---------------------------------------------------------
\begin{frame}[fragile]{Bước 2 --- khung JUnit test class}
\begin{lstlisting}[language=java,basicstyle=\ttfamily\scriptsize]
class LoginTest {

  static final String LOGIN_URL =
      "http://localhost:5173/login";

  WebDriver driver;

  @BeforeEach
  void setUp() {
    driver = new ChromeDriver();
    driver.manage().window().setSize(
        new Dimension(1280, 800));
    driver.get(LOGIN_URL);
  }

  @AfterEach
  void tearDown() { driver.quit(); }
}
\end{lstlisting}
\end{frame}

%--- SLIDE 41 ---------------------------------------------------------
\begin{frame}[fragile]{Bước 3 --- TC-L01: đăng nhập Org Admin}
\begin{lstlisting}[language=java,basicstyle=\ttfamily\scriptsize]
@Test
void dangNhapOrgAdmin_veDashboard() {
  new Select(driver.findElement(
      By.id("login-role")))
      .selectByValue("org_admin");
  // email da tu dien theo role - khong can go

  driver.findElement(By.id("login-submit")).click();

  assertTrue(driver.getCurrentUrl()
      .contains("/dashboard"));   // XANH ngay!
}
\end{lstlisting}

  \begin{block}{Vì sao assert URL ngay sau \texttt{click()} vẫn xanh?}
    Handler submit của React gọi \texttt{navigate()} \textbf{đồng bộ} ngay
    trong sự kiện click --- URL đã đổi trước khi \texttt{click()} trả về.
    Nhưng đừng vội mừng\ldots{} URL không phải là tất cả (slide sau).
  \end{block}
\end{frame}

%--- SLIDE 42 ---------------------------------------------------------
\begin{frame}[fragile]{TC-L02: lúc ĐỎ lúc XANH?! --- test ``chập chờn''}
\begin{lstlisting}[language=java,basicstyle=\ttfamily\scriptsize]
@Test
void dangNhapAnalyst_thayHopBaoCao() {
  new Select(driver.findElement(By.id("login-role")))
      .selectByValue("analyst");
  driver.findElement(By.id("login-submit")).click();

  assertTrue(driver.getCurrentUrl()
      .contains("/soc/inbox"));       // luon XANH
  driver.findElement(By.xpath(
      "//button[@role='tab'][contains(.,'THREAT')]"));
  // lan dau: NoSuchElementException - DO
  // chay lai: co khi lai XANH ?!
}
\end{lstlisting}

  \begin{itemize}
    \item URL đổi \textbf{đồng bộ}; nội dung trang đích
          (\texttt{lazy(() => import\ldots)} + gọi API) đến \textbf{sau}
    \item Test \textbf{đua} với trang render: thắng xanh, thua đỏ ---
          \emph{flaky test}, nguy hiểm hơn test đỏ hẳn (buổi 11)
  \end{itemize}
\end{frame}

%--- SLIDE 43 ---------------------------------------------------------
\begin{frame}[fragile]{Giải pháp tạm thời --- và vấn đề để lại cho buổi 11 (1/2)}
\begin{lstlisting}[language=java,basicstyle=\ttfamily\scriptsize]
@Test
void dangNhapAnalyst_thayHopBaoCao()
    throws InterruptedException {  // <-- nho them throws!
  new Select(driver.findElement(By.id("login-role")))
      .selectByValue("analyst");
  driver.findElement(By.id("login-submit")).click();

  // TAM THOI: cho "cung" 1.5 giay
  Thread.sleep(1500);

  driver.findElement(By.xpath(
      "//button[@role='tab'][contains(.,'THREAT')]"));
  // tim thay -> XANH
}
\end{lstlisting}
\end{frame}

%--- SLIDE 43b --------------------------------------------------------
\begin{frame}{Giải pháp tạm thời --- và vấn đề để lại cho buổi 11 (2/2)}
  \begin{alertblock}{\texttt{Thread.sleep} là anti-pattern}
    \begin{itemize}
      \item Máy nhanh: \textbf{lãng phí} 0.9s mỗi test $\times$ hàng trăm test
      \item Máy chậm / CI quá tải: 1.5s vẫn \textbf{không đủ} $\to$ test chập chờn (flaky)
      \item Con số 1500 là ``bói'' --- không gắn với điều kiện thật nào
    \end{itemize}
  \end{alertblock}

  \begin{exampleblock}{Câu hỏi treo cho Buổi 11}
    Làm sao chờ \textbf{đúng sự kiện} ``element đã xuất hiện'' thay vì chờ
    thời gian?
    $\to$ \texttt{WebDriverWait} + \texttt{ExpectedConditions} (explicit wait).
  \end{exampleblock}
\end{frame}

%--- SLIDE 44 ---------------------------------------------------------
\begin{frame}[fragile]{Gom điểm chung --- helper \texttt{dangNhap(role)}}
\begin{lstlisting}[language=java,basicstyle=\ttfamily\scriptsize]
void dangNhap(String role) {
  new Select(driver.findElement(By.id("login-role")))
      .selectByValue(role);
  driver.findElement(By.id("login-submit")).click();
}

@Test
void dangNhapLearner_veLearn()
    throws InterruptedException {
  dangNhap("learner");
  Thread.sleep(1500);
  assertTrue(driver.getCurrentUrl()
      .contains("/learn"));
}
\end{lstlisting}

  {\small Mỗi role một dòng \texttt{dangNhap("...")} --- hết lặp code.
  Đây chính là bước đệm cho \textbf{Page Object Model} (buổi 11): gom
  locator + thao tác của một trang vào một class.}
\end{frame}

%--- SLIDE 45 ---------------------------------------------------------
\begin{frame}[fragile]{TC-L03 --- kịch bản tiêu cực: email sai định dạng}
\begin{lstlisting}[language=java,basicstyle=\ttfamily\scriptsize]
@Test
void emailSaiDinhDang_oLaiTrangLogin()
    throws InterruptedException {
  WebElement email =
      driver.findElement(By.id("login-email"));
  email.clear();
  email.sendKeys("khong-phai-email");
  driver.findElement(By.id("login-submit")).click();
  Thread.sleep(500);

  assertTrue(driver.getCurrentUrl()
      .contains("/login"));   // XANH
}
\end{lstlisting}

  \begin{block}{Vì sao vẫn ở \texttt{/login}?}
    \texttt{type="email" required} $\to$ \textbf{HTML5 constraint validation}
    chặn submit; handler React không chạy. Message (đọc qua
    \texttt{getAttribute("validationMessage")}) do browser sinh --- đừng
    assert nội dung!
  \end{block}
\end{frame}

%--- SLIDE 46 ---------------------------------------------------------
\begin{frame}[fragile]{TC-L04 --- F5 sau đăng nhập: test tìm ra ``chuyện thú vị''}
\begin{lstlisting}[language=java,basicstyle=\ttfamily\scriptsize]
@Test
void f5SauDangNhap_giuPhien()
    throws InterruptedException {
  dangNhap("analyst");
  Thread.sleep(1500);

  driver.navigate().refresh();   // F5
  Thread.sleep(1000);
  // Ky vong: van o /soc/inbox.  Thuc te: DO!
  assertTrue(driver.getCurrentUrl()
      .contains("/soc/inbox"));  // dang o /login
}
\end{lstlisting}

  {\small \textbf{Test fail --- nhưng là phát hiện giá trị}: phiên dev nằm
  \textbf{trong bộ nhớ JS} --- F5 là mất, app về \texttt{/login}. By design:
  bản Cognito lưu phiên trong \texttt{localStorage}, F5 \textbf{giữ} phiên.\\[0.25em]
  \textbf{Luật rút ra}: giữa kịch bản \textbf{không} \texttt{driver.get()}
  (= reload = văng phiên) --- điều hướng \textbf{qua UI}. Đây là lý do POM
  của module \texttt{:e2e} click sidebar (buổi 11).}
\end{frame}

%=====================================================================
\section{Debug test Selenium}
%=====================================================================

%--- SLIDE 47 ---------------------------------------------------------
\begin{frame}[fragile]{Debug --- chụp screenshot khi test fail}
\begin{lstlisting}[language=java,basicstyle=\ttfamily\scriptsize]
import org.openqa.selenium.OutputType;
import org.openqa.selenium.TakesScreenshot;

// Ep kieu driver sang TakesScreenshot
File shot = ((TakesScreenshot) driver)
    .getScreenshotAs(OutputType.FILE);

Files.copy(shot.toPath(),
    Path.of("build/loi-dang-nhap.png"),
    StandardCopyOption.REPLACE_EXISTING);
\end{lstlisting}

  \begin{block}{Vì sao cần?}
    \small
    Test E2E fail trong CI lúc 2 giờ sáng --- không ai nhìn thấy browser.
    Screenshot tại thời điểm fail là \textbf{bằng chứng} quan trọng nhất
    để chẩn đoán (trang trắng? lỗi 403? nút bị che?).
  \end{block}

  {\small \textbf{Nâng cao (tự tìm hiểu)}: gói chụp màn hình vào
  \texttt{@AfterEach} chỉ khi test fail (JUnit \texttt{TestWatcher}) ---
  sẽ gặp lại ở buổi 11--12 khi nói về CI.}
\end{frame}

%--- SLIDE 48 ---------------------------------------------------------
\begin{frame}[fragile]{Debug --- page source và DevTools}
\begin{lstlisting}[language=java]
// Toan bo HTML tai thoi diem goi -
// xem element co thuc su ton tai khong
String html = driver.getPageSource();
System.out.println(
    html.contains("login-email"));  // true?
\end{lstlisting}

  \begin{block}{Quy trình tìm selector bằng Chrome DevTools}
    \begin{enumerate}
      \item Mở trang $\to$ F12 (hoặc chuột phải $\to$ Inspect)
      \item Bấm biểu tượng mũi tên, trỏ vào element cần tìm
      \item Đọc DOM: có \texttt{id}? có thuộc tính đặc trưng?
      \item Thử selector ngay trong Console:
            \texttt{document.querySelector("...")} hoặc
            Ctrl+F trong tab Elements (dán được cả XPath)
      \item Chỉ khi selector ``ăn'' đúng 1 element mới đưa vào test
    \end{enumerate}
  \end{block}
\end{frame}

%--- SLIDE 49 ---------------------------------------------------------
\begin{frame}{Tổng hợp --- bộ kỹ năng Selenium sau buổi hôm nay}
  \begin{center}
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{>{\raggedright\arraybackslash}p{3.3cm}>{\raggedright\arraybackslash}p{8.2cm}}
    \toprule
    \textbf{Nhóm} & \textbf{API đã học} \\
    \midrule
    Vòng đời & \texttt{new ChromeDriver()}, \texttt{quit()} (so với \texttt{close()}) \\
    Điều khiển browser & \texttt{get}, \texttt{getTitle}, \texttt{getCurrentUrl}, \texttt{navigate()}, \texttt{manage().window()} \\
    Tìm element & \texttt{findElement(s)} + \texttt{By.id / cssSelector / xpath / tagName\ldots} \\
    Thao tác & \texttt{sendKeys}, \texttt{clear}, \texttt{click}, \texttt{Select.selectByValue} \\
    Đọc / kiểm tra & \texttt{getText}, \texttt{getAttribute}, \texttt{isDisplayed}, \texttt{isEnabled} \\
    Debug & \texttt{TakesScreenshot}, \texttt{getPageSource}, DevTools \\
    \bottomrule
  \end{tabular}
  \end{center}

  {\small \textbf{Điểm yếu còn lại của bộ test hôm nay}: \texttt{Thread.sleep}
  rải khắp nơi. Buổi 11 sẽ thay bằng \textbf{explicit wait}, gom locator vào
  \textbf{Page Object Model}, và chạy \textbf{headless} cho CI.}
\end{frame}

%=====================================================================
\section{Thực hành và bài nộp}
%=====================================================================

%--- SLIDE 50 ---------------------------------------------------------
\begin{frame}{Thực hành trên lớp}
  \begin{block}{Bài 1 --- Dựng và chạy bộ test đăng nhập (bắt buộc)}
    Tạo project \texttt{digishield-e2e-btl} (Maven hoặc Gradle), chạy backend
    dev + frontend, hoàn thiện \texttt{LoginTest} với 3 test \textbf{xanh}:
    Org Admin (TC-L01), Analyst + tab THREAT (TC-L02), email sai định dạng
    (TC-L03) --- và TC-L04 kèm \textbf{nhận xét} vì sao đỏ.
  \end{block}

  \begin{block}{Bài 2 --- Mở rộng vai trò}
    Dùng helper \texttt{dangNhap(role)}, viết thêm 3 test:
    \texttt{learner} $\to$ \texttt{/learn},
    \texttt{super\_admin} $\to$ \texttt{/super/tenants},
    \texttt{content\_editor} $\to$ \texttt{/dashboard}.
  \end{block}

  \begin{block}{Bài 3 --- Trinh sát locator cho buổi 11}
    Đăng nhập Analyst, dùng DevTools tìm selector cho: ô tìm kiếm SOC Inbox
    (theo placeholder), một dòng trong danh sách báo cáo. Chụp screenshot sau
    đăng nhập bằng \texttt{TakesScreenshot} và lưu file.
  \end{block}
\end{frame}

%--- SLIDE 51 ---------------------------------------------------------
\begin{frame}{Bài nộp (về nhà --- gắn với Bài tập lớn)}
  \begin{block}{Yêu cầu: bộ kịch bản Selenium cho chức năng đăng nhập}
    Áp dụng cho \textbf{ứng dụng của nhóm} (BTL) --- nhóm nào dùng DigiShield thì
    làm trên DigiShield:
    \begin{enumerate}\small
      \item Bảng \textbf{đặc tả test case} đăng nhập theo khuôn mẫu buổi 9:
            tối thiểu 5 ca (2 tích cực, 3 tiêu cực)
      \item Project test riêng (Maven/Gradle) cài Selenium 4.x + JUnit 5,
            tự động hóa đủ 5 ca, có \texttt{@BeforeEach}/\texttt{@AfterEach}
      \item Nhận xét về \textbf{locator} của ứng dụng nhóm: element nào thiếu
            id ổn định, đề xuất \texttt{data-testid} ở đâu
      \item \texttt{README.md}: cách chạy hệ thống + cách chạy test
    \end{enumerate}
  \end{block}

  \begin{alertblock}{Nộp}
    Đẩy lên repo nhóm trước \textbf{buổi 11}. Đây là thành phần
    ``Selenium E2E'' trong báo cáo BTL (A1.2 --- 30\% điểm học phần).
  \end{alertblock}
\end{frame}

%--- SLIDE 52 ---------------------------------------------------------
\begin{frame}{Tổng kết và buổi tới}
  \begin{columns}[T]
    \column{0.5\textwidth}
    \textbf{Hôm nay đã học:}
    \begin{itemize}
      \item Vị trí E2E trên testing pyramid: ít --- đắt --- giá trị cao
      \item Selenium: lịch sử, WebDriver/IDE/Grid, kiến trúc W3C
      \item Cài đặt: \texttt{selenium-java} 4.x + JUnit 5, Selenium Manager
      \item Locators: ưu tiên id \(>\) CSS \(>\) XPath; \texttt{data-testid}
      \item Bộ test đăng nhập DigiShield 4 kịch bản
    \end{itemize}

    \column{0.5\textwidth}
    \begin{block}{Buổi 11 --- Selenium (phần 2)}
      \begin{itemize}
        \item \textbf{Explicit wait}: \texttt{WebDriverWait}, \texttt{ExpectedConditions} --- xóa sổ \texttt{Thread.sleep}
        \item \textbf{Page Object Model} --- tổ chức code test
        \item Kịch bản E2E nhiều trang (SOC Inbox, Watchlist)
        \item Chạy \textbf{headless} chuẩn bị cho CI
      \end{itemize}
    \end{block}
    \begin{exampleblock}{Chuẩn bị}
      Hoàn thành bài nộp; giữ project \texttt{digishield-e2e} chạy được.
    \end{exampleblock}
  \end{columns}
\end{frame}

%=====================================================================
\section{Tài liệu tham khảo}
%=====================================================================

%--- SLIDE 53 ---------------------------------------------------------
\begin{frame}{Tài liệu tham khảo}
  \vspace*{-0.4em}
  \setbeamertemplate{bibliography item}{[\theenumiv]}
  \footnotesize
  \begin{thebibliography}{9}
    \bibitem{slide-bm}
    \textit{Slide bài giảng điện tử môn Kiểm thử Phần mềm}.

    \bibitem{ammann-offutt}
    Paul Ammann, Jeff Offutt,
    \textit{Introduction to Software Testing}, Cambridge University Press, 2008.

    \bibitem{myers}
    Glenford J. Myers,
    \textit{The Art of Software Testing}, 2nd ed., Wiley, 2004.

    \bibitem{istqb-exam}
    ISTQB Foundation Level Syllabus, version 4.0, 2023.
    \url{https://istqb.org/}

    \bibitem{selenium-docs}
    Selenium Project, \textit{Selenium Documentation},
    \url{https://www.selenium.dev/documentation/}.

    \bibitem{w3c-webdriver}
    W3C, \textit{WebDriver Specification},
    \url{https://www.w3.org/TR/webdriver/}.

    \bibitem{digishield-src}
    Mã nguồn thực hành DigiShield: frontend
    (\texttt{LoginPage.tsx}, \texttt{router.tsx}) và backend Spring Modulith.
  \end{thebibliography}
\end{frame}

%=====================================================================
\end{document}
