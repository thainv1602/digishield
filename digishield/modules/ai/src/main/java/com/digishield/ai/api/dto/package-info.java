/**
 * Public DTOs of the AI module (e.g. {@code SimTemplateView}). Exposed as a
 * Spring Modulith named interface so callers of the AI API (the application
 * shell) can consume the returned views across the module boundary.
 */
@org.springframework.modulith.NamedInterface("dto")
package com.digishield.ai.api.dto;
