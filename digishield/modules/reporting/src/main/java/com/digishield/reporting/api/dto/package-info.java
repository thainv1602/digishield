/**
 * Public DTOs of the reporting module (e.g. {@code PhishingReportDto}). Exposed
 * as a Spring Modulith named interface so callers of the reporting API (the
 * application shell) can consume the returned views across the module boundary.
 */
@org.springframework.modulith.NamedInterface("dto")
package com.digishield.reporting.api.dto;
